import 'package:flutter/material.dart';

import '../../../../../core/pages/coming_soon.dart';

class Accounting extends StatelessWidget {
  const Accounting({super.key});

  @override
  Widget build(BuildContext context) {
    return ComingSoon(
      title: "ACCOUNTING",
    );
  }
}
